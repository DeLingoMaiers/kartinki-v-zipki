name: Images to ZIP

on:
  workflow_dispatch:
    inputs:
      gist_id:
        description: 'Gist ID'
        required: true
      folder_name:
        description: 'Output folder name'
        required: false
        default: 'export'

jobs:
  create-zip:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Create ZIP and save to Gist
        env:
          GIST_ID: ${{ github.event.inputs.gist_id }}
          FOLDER_NAME: ${{ github.event.inputs.folder_name }}
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          pip install requests

          python3 << 'EOF'
          import os
          import json
          import base64
          import requests
          import zipfile
          import concurrent.futures

          GIST_ID = os.environ['GIST_ID']
          GH_TOKEN = os.environ['GH_TOKEN']
          FOLDER_NAME = os.environ.get('FOLDER_NAME', 'export')
          
          headers = {'Authorization': f'token {GH_TOKEN}'}

          print(f"Fetching Gist: {GIST_ID}")
          gist_resp = requests.get(f'https://api.github.com/gists/{GIST_ID}', headers=headers)
          gist_resp.raise_for_status()
          gist_data = gist_resp.json()
          
          images_content = gist_data['files']['images.json']['content']
          images = json.loads(images_content)
          
          print(f"Processing {len(images)} images...")

          def download_image(item):
              url = item.get('url')
              name = item.get('name', 'image')
              img_type = item.get('type', 'main')
              
              if not url:
                  return None
              
              try:
                  r = requests.get(url, timeout=60, allow_redirects=True)
                  if r.status_code == 200:
                      content_type = r.headers.get('content-type', '')
                      ext = '.png'
                      if 'jpeg' in content_type or 'jpg' in content_type:
                          ext = '.jpg'
                      elif 'webp' in content_type:
                          ext = '.webp'
                      elif 'svg' in content_type:
                          ext = '.svg'
                      
                      filename = f"{name}_{img_type}{ext}"
                      filename = "".join(c for c in filename if c.isalnum() or c in '._- ').strip()
                      return (filename, r.content)
              except Exception as e:
                  print(f"Error downloading {name}: {e}")
              return None

          print("Downloading images...")
          with concurrent.futures.ThreadPoolExecutor(max_workers=30) as executor:
              results = list(executor.map(download_image, images))
              downloaded = [r for r in results if r is not None]

          print(f"Downloaded {len(downloaded)} of {len(images)} images")

          result = {'success': False, 'status': 'failed'}
          zip_base64 = None

          if len(downloaded) == 0:
              result = {'success': False, 'error': 'No images downloaded', 'status': 'failed'}
          else:
              zip_path = f'/tmp/{FOLDER_NAME}.zip'
              print("Creating ZIP...")
              with zipfile.ZipFile(zip_path, 'w', zipfile.ZIP_DEFLATED) as zf:
                  for filename, content in downloaded:
                      zf.writestr(filename, content)

              zip_size = os.path.getsize(zip_path)
              print(f"ZIP created: {zip_size / 1024 / 1024:.2f} MB")

              with open(zip_path, 'rb') as f:
                  zip_base64 = base64.b64encode(f.read()).decode('utf-8')
              
              result = {
                  'success': True,
                  'status': 'completed',
                  'fileName': f'{FOLDER_NAME}.zip',
                  'filesCount': len(downloaded),
                  'totalFiles': len(images),
                  'fileSize': zip_size,
                  'fileSizeMB': round(zip_size / 1024 / 1024, 2)
              }
              
              print(f"ZIP encoded: {len(zip_base64)} chars")

          print("Saving to Gist...")
          files_to_update = {
              'result.json': {'content': json.dumps(result, indent=2)}
          }
          
          if zip_base64:
              files_to_update['archive.zip.b64'] = {'content': zip_base64}
          
          update_resp = requests.patch(
              f'https://api.github.com/gists/{GIST_ID}',
              headers=headers,
              json={'files': files_to_update}
          )
          
          if update_resp.status_code == 200:
              print("Saved to Gist!")
          else:
              print(f"Failed: {update_resp.status_code} - {update_resp.text[:500]}")

          print(json.dumps(result, indent=2))
          EOF
